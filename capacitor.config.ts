import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: '5874-technical-challenge',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
